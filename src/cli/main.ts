// CLI entry — bootstrap stub.
//
// Full implementation (argv parsing, --transport stdio|http, --skills-dir,
// --tree-file, --port, server start) lands in commit #18.
console.error(
  "[acosmi-skill-agent-mcp] bootstrap stub — CLI lands in commit #18.",
);
process.exit(1);
