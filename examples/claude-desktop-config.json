{
  "mcpServers": {
    "acosmi-skill-agent": {
      "command": "npx",
      "args": [
        "-y",
        "@acosmi/skill-agent-mcp",
        "--transport",
        "stdio",
        "--skills-dir",
        "/absolute/path/to/your/skills"
      ],
      "env": {
        "ANTHROPIC_API_KEY": "<set this if any of your SKILLs need it>"
      }
    }
  }
}
